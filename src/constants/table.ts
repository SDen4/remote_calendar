import { IData } from '../store/types';

export const initialData: IData[] = [
  {
    name: 'Total office',
  },
  {
    name: 'Total remote',
  },
];

export const defaultCell = {
  row: '',
  cell: '',
};
